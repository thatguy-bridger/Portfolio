Circular button for a single glyph — settings gear, theme toggle, drag handle actions.

```jsx
<IconButton icon="⚙" tone="accent" onClick={openSettings} title="Settings" />
```

`tone`: `default` (bordered surface), `accent` (indigo fill), `danger` (red fill). `size` in px, default 44 (meets tap-target minimum).
