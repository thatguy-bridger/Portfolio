Small pill label for status/category tags — template names, section types, publish status.

```jsx
<Badge color="purple">Draft</Badge>
```

Colors map to the multi-accent palette: `indigo` `purple` `orange` `pink` `green` `red`.
