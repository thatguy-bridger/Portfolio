Text field with optional label; indigo focus ring matches the brand accent.

```jsx
<Input label="Site title" placeholder="Jane Doe — Portfolio" value={title} onChange={e=>setTitle(e.target.value)} />
```
