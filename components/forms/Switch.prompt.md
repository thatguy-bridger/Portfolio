Segmented control — theme switch (Light/Dark/System) in the source app, reusable for any 2-3 option toggle.

```jsx
<Switch options={['Light','Dark','System']} value={theme} onChange={setTheme} />
```
