Centered dialog over a dark scrim — settings panels, confirmations, publish flow.

```jsx
<Modal open={open} title="Edit Section" onClose={close} footer={<><Button variant="ghost" onClick={close}>Cancel</Button><Button onClick={save}>Save</Button></>}>
  form fields…
</Modal>
```
