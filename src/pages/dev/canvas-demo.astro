---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { CanvasDemo } from '../../components/dev/CanvasDemo';
---

<!--
  Dev-only harness for the Phase 2 block registry + freeform canvas —
  see QA_CHECKLIST.md's Phase 2 section. Entirely local-state, no Firebase/
  Supabase calls (see CanvasDemo.tsx's header comment). Not linked from any
  real page.

  Styling comes entirely from BaseLayout's theme.css (the design-system
  tokens every block component references — --surface-card, --accent-primary,
  --radius-md, etc. — are now real, ported in Phase 1). This page used to
  carry its own fallback token block for standalone testing before that
  merged; removed now that it's redundant.
-->

<BaseLayout title="Canvas demo (dev) — Portfolio">
  <CanvasDemo client:only="react" />
</BaseLayout>
